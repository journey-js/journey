let config = {
	fallback: null,

	base: '',

	useHash: true,

	useOnHashChange: false,

	hash: '#',

	defaultRoute: null,

	target: null,
	
	debug: true,
	
	abuseTimeout: 1000
};

export default config;
